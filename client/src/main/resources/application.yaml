eureka:
    instance:
      hostname: localhost
    client:
      serviceUrl:
        defaultZone: http://localhost:8761/eureka/
spring:
  application: 
    name: Kafka_Client_app
    
server:
  servlet:
    context-path: /clientApp  
  port: 8081
